#include <stdio.h>
int main() 
{
    int n;
    printf("Enter a positive integer n: ");
    scanf("%d", &n);

    int sum = 0;
    for (int i = 1; i <= n; i += 2)
    {
        sum += i * i;
    }

    printf("The sum of squares of even numbers up to %d is %d\n", n, sum);

    return 0;
}
OUTPUT:
Enter a positive integer n: 5
The sum of squares of even numbers up to 5 is 35
